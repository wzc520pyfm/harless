{
  "name": "harless-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
